name: Frontend Tests
on:
  pull_request
jobs:
  check_paths:
    runs-on: ubuntu-latest
    outputs:
      if_run: ${{ steps.filter.outputs.frontend }}
    steps:
      - uses: actions/checkout@v4
      - uses: dorny/paths-filter@v3.0.2
        id: filter
        with:
          filters: |
            frontend:
              - 'src/frontend/**'
              - '.github/workflows/frontend-tests.yaml'
  test:
    name: Test Frontend
    needs: check_paths
    if: ${{ needs.check_paths.outputs.if_run == 'true' }}
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./src/frontend

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install UV
        uses: astral-sh/setup-uv@v5

      - name: Set up Python
        run: uv python install

      - name: Install dependencies
        run: |
          uv sync --locked --dev

      - name: Lint with Ruff
        run: |
          uv run ruff check --output-format=github --target-version=py313 src frontend-tests
          uv run ruff format --diff --check --target-version=py313 src frontend-tests
